{
  "name": "nikke-raid-autosync-spa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "NIKKE Union Raid auto-sync SPA — postMessage receiver + Google Sheets batchUpdate (E-NRA-002, third-party tool for ssissun's union)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/google.picker": "^0.0.42",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vite-plugin-singlefile": "^2.0.3",
    "vitest": "^2.1.4"
  },
  "engines": {
    "node": ">=20"
  }
}
